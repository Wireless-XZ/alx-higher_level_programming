#!/usr/bin/python3
def search_replace(my_list, search, replace):
    new = my_list.copy()
    return [new[j - 1] = replace for j in new if j == search]
#for j in new:
 #       if j == search:
  #          new[j - 1] = replace
   # return new
