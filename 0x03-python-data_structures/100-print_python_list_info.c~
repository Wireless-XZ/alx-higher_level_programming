BB#include <stdio.h>
#include <Python.h>
/**
 * print_python_list_info - prints some basic info about Python lists
 * @p: python list
 *
 * Return: void
 */
void print_python_list_info(PyObject *p)
{
	printf("[*] TYPE of the Python obj = %s\n", ((PyObject*)(o))->ob_type);
}
